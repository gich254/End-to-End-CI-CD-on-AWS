version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "arn:aws:ecs:us-east-1:<account_id>:task-definition/myapp-task:1"
        LoadBalancerInfo:
          ContainerName: "myapp-container"
          ContainerPort: 3000
